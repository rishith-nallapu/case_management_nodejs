// Node apps are purely logic based
// with this we create server side app     toggle nav     and clear to clear terminal
// with this we can access filesytem and know about our operating system
// browser depends on user's browser whereas node app depends on nodejs
// To get node to evaluate our code we have two options repl(read eval print loop) and CLI executable

//****To start with repl we just type node in terminal and  inorder to close (ctrl+c)
//*****For mid to high range applications we use CLI 
// *****To run type (node filename) or else simply run   (ctrl+`)--to directly get terminal

// Global variables in node---- we can accesss any where
// **In node we don't have window object as no browser
// __dirname = path to current directory
// __filename = filename
// require = function to use modules(in common js)
// module = info about cuurent module or file
// process = info about env where the program is being executed
// ***** In order to go to previous cmd click(arrowup)

// setInterval(()=>{
//     console.log("hello")
// },1000)

// commonjs,  every file is by default a module
// module----encapsulated code(only share minimum)
// if give value to exports object then the values can be shared to different files
// in order to create own module we use require and './filename where we wrote moduleexports'
// module.exports.name to directly add

//  Built in modules (OS,FS(filesystem),PATH,HTTP)  bult-in modules










