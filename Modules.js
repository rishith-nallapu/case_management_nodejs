const name=require('./NT1')
const l=require('./NtII')
const thing=require('./Alternativesyntax')
require('./NtII')
//console.log(thing)  // { items: [ 'toy', 'brush' ], Personname: { name: 'ram' } }
//console.log(name)
// l('john')
// l(name.ram)
// l(name.sita)

// although we not assigned we get the value because we are calling the function in Nt3 module
